#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 for a successful program
 */
int main(void)
{
int lowerCase = 'a';
while (lowerCase <= 'z')
{
if (lowerCase == 'o' || lowerCase == 'q')
{
lowerCase++;
}
else
}
putchar(lowerCase);
putchar('\n');
}
lowerCase++;
}
return (0);
}
