#include <stdio.h>
/**
 * main - A program that print using printf function
 * Return: 0 when the condition is true, the line will be printed
 */
int main(void)
{
	print("with proper grammar, but the outcome is a piece of art,\n")
	return (0);
}
