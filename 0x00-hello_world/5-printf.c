#include <stdio.h>
/**
 * main - A program that print a line using the printf function
 * Return: 0 is success when the condition is true and the line will be printed
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
